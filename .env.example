# GitHub Personal Access Token (GHP) — must have 'contents: write' permission
# Used to commit: (1) token JSON data and (2) an AI-built HTML webpage per spin
# Both files land in the repo; the HTML page is also served via GitHub Pages
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_OWNER=www-infinity
GITHUB_REPO=Open-Flaw
PORT=3000

# AI LLM proxy — kept server-side so your API key is never exposed to the browser
# Works with OpenAI, OpenRouter, Groq, or any OpenAI-compatible endpoint
AI_API_KEY=your_ai_api_key_here
AI_API_URL=https://api.openai.com/v1/chat/completions
AI_MODEL=gpt-3.5-turbo
